/* eslint-disable @typescript-eslint/no-explicit-any */
const createBookingService = () => {
    // Booking service implementation

}

const getBookingDetailsService = (bookingId: string) =>    {
    // Get booking details implementation
}
const getMyBookingDetailsService = (bookingId: string) =>    {
    // Get booking details implementation
}
const getOneBookingDetailsService = (bookingId: string) =>    {
    // Get booking details implementation
}
const updateBookingService = (bookingId: string, updateData: any) => {
    // Update booking implementation
}
const cancelBookingService = (bookingId: string) => {
    // Cancel booking implementation
}

export const BookingService = {
    createBookingService,
    getBookingDetailsService,
    getMyBookingDetailsService,
    getOneBookingDetailsService,
    updateBookingService,
    cancelBookingService,

}